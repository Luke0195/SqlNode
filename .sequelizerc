// Credencias de acesso do sequelize
const path = require('path');

module.exports ={
 config:path.resolve(__dirname, 'src', 'config', 'database.js'),// arquivo de configuração das credencias do banco
 'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations')
}